---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: bonzofenix/ubuntu-ci-tools
    tag: edge

params:

inputs:
  - name: pipeline
  - name: config

outputs:
  - name: config-mod

run:
  path: pipeline/ci/scripts/bosh/update_runtime_config.sh

